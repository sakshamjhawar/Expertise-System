

host=localhost
username = root
password = ""
database name = test


commands:

use test;

create table faculty_mem(fid int,inst varchar(20),
sub varchar(20),sdate date,edate date,up_pg varchar(20),ctype varchar(20),fback varchar(20),details varchar(20));